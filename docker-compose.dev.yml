services:
  app:
    volumes:
      - .:/var/www
      - /var/www/vendor
      - /var/www/node_modules

  nginx:
    volumes:
      - .:/var/www
      - ./docker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf
